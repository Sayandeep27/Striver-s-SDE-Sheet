class Solution {
public:
    bool rotateString(string s, string t) {
        
      int n=s.size();
      int m=t.size();

      if(s==t) return true;

      if(n!=m) return false;

      t=t+t;

      int found=t.find(s);

      if(t.size()>=found) return true;


      return false;


    }
};

class Solution {
public:
int maxProduct(vector<int>&v){
int ans=INT_MIN,n=v.size(),sum=1;
for(int i=0;i<n;i++){
sum*=v[i];
ans=max(ans,sum);
if(sum==0)sum=1;
}
sum=1;
for(int i=n-1;i>=0;i--){
sum*=v[i];
ans=max(ans,sum);
if(sum==0)sum=1;
}
return ans;
}
};
